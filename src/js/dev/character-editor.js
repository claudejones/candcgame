(() => {
  "use strict";
  if(!window.CC_APP?.isDevelopment)throw new Error("Character editor requires development bootstrap");const ATLASES={run:"../assets/characters/G1A_RUN_CYCLE_ATLAS.png",idle:"../assets/characters/G1B_IDLE_ATLAS.png",jump:"../assets/characters/G1C_JUMP_ATLAS.png",slide:"../assets/characters/G1D_SLIDE_ATLAS.png",hit:"../assets/characters/G1E_HIT_ATLAS.png",celebrate:"../assets/characters/G1F_CELEBRATE_ATLAS.png"};let character="claude",state="run",frame=0;
  function apply(){if(window.CC_APP.mode!=="design"||window.CC_DESIGN_SELECTION.current.type!=="character")return;const w=window.CC_RUNTIME_API?.win();if(!w?.GAME_CONFIG)return;const g=window.CC_DESIGN_DRAFT.getGlobals().character,s=window.CC_DESIGN_SELECTION.current,stage=window.CC_DESIGN_DRAFT.getStage(s.stageId);window.CC_RUNTIME_API.setStage(s.stageId);w.GAME_CONFIG.masterScale[character]=g.masterScale[character];w.GAME_CONFIG.stateScale[character][state]=g.stateScale[character][state];w.GAME_CONFIG.renderOffsetX[character][state]=g.renderOffsetX[character][state];w.GAME_CONFIG.renderOffsetY[character][state]=g.renderOffsetY[character][state];w.GAME_CONFIG.cropInsets[character][state]=JSON.parse(JSON.stringify(g.cropInsets[character][state]));w.GAME_CONFIG.objectQA.characterCollision[character][state]={...g.collision[character][state]};w.GAME_CONFIG.worldProfiles[s.stageId].characterGrounding[character]=stage.character.grounding[character];window.CC_RUNTIME_API.setCharacter(character);window.CC_RUNTIME_API.setCharacterState(state);decorate();}
  function decorate(){const s=window.CC_DESIGN_SELECTION.current;if(window.CC_APP.mode!=="design"||s.type!=="character")return;const panel=document.getElementById("phase7ContextualInspector");if(!panel)return;panel.querySelector(".character-atlas-view")?.remove();const g=window.CC_DESIGN_DRAFT.getGlobals().character,crop=g.cropInsets[character][state][frame],box=document.createElement("section");box.className="inspector-group character-atlas-view";box.innerHTML="<h3>STATE / SOURCE CROP</h3>";const canvas=document.createElement("canvas");canvas.className="atlas-canvas";const img=new Image();img.onload=()=>{const cell=700,row=character==="claude"?0:1,sx=frame*cell,sy=row*cell,scale=Math.min(260/cell,180/cell);canvas.width=Math.round(cell*scale);canvas.height=Math.round(cell*scale);const c=canvas.getContext("2d");c.drawImage(img,sx,sy,cell,cell,0,0,canvas.width,canvas.height);c.strokeStyle="#ffdf5d";c.lineWidth=2;c.strokeRect(crop.l*scale+.5,crop.t*scale+.5,(cell-crop.l-crop.r)*scale-1,(cell-crop.t-crop.b)*scale-1);};img.src=ATLASES[state];const cap=document.createElement("p");cap.className="inspector-note";cap.textContent=`${character.toUpperCase()} • ${state.toUpperCase()} • frame ${frame+1} • yellow = crop bounds`;box.append(canvas,cap);panel.insertBefore(box,panel.querySelector(".inspector-actions"));}
  function select(who,nextState,nextFrame=0){character=who;state=nextState;frame=Math.max(0,Math.min((window.GAME_CONFIG.state[state]?.frames||1)-1,nextFrame));document.dispatchEvent(new CustomEvent("cc-character-selection"));apply();}function selectFrame(next){frame=Math.max(0,Math.min((window.GAME_CONFIG.state[state]?.frames||1)-1,next));document.dispatchEvent(new CustomEvent("cc-character-selection"));apply();}window.CC_CHARACTER_EDITOR=Object.freeze({get character(){return character;},get state(){return state;},get frame(){return frame;},select,selectFrame,refresh:apply});window.CC_DESIGN_SELECTION.onChange(apply);window.CC_DESIGN_DRAFT.onChange(apply);window.CC_APP.onModeChange(apply);document.addEventListener("cc-inspector-rendered",decorate);window.CC_RUNTIME_API?.frame()?.addEventListener("load",apply);
})();
