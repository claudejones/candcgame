(() => {
  "use strict";
  if(!window.CC_APP?.isDevelopment)throw new Error("Runtime Monitor requires development bootstrap");
  function mount(){if(document.getElementById("phase7RuntimeMonitor"))return;const panel=document.createElement("section");panel.id="phase7RuntimeMonitor";panel.className="runtime-monitor collapsed";const head=document.createElement("button");head.type="button";head.className="runtime-monitor-toggle";head.innerHTML="<strong>RUNTIME MONITOR</strong><span>+</span>";const body=document.createElement("div");body.className="runtime-monitor-body";panel.append(head,body);document.body.appendChild(panel);head.onclick=()=>{panel.classList.toggle("collapsed");head.querySelector("span").textContent=panel.classList.contains("collapsed")?"+":"−";};function render(){panel.hidden=window.CC_APP.mode==="game";if(panel.hidden)return;const s=window.CC_RUNTIME_API?.getState();if(!s)return;body.innerHTML=`<div><b>Build</b><span>${s.build}</span></div><div><b>Stage</b><span>${s.stageLabel}</span></div><div><b>Character</b><span>${s.character} / ${s.characterState} / frame ${s.frame}</span></div><div><b>Ground</b><span>canonical Y=${window.GAME_SCHEMA.GROUND_SURFACE_Y} • rendered ${s.renderedSurface}</span></div><div><b>World</b><span>scroll ${s.scroll} • ${s.spawnTime}</span></div><div><b>Hazard</b><span>${s.hazard} • ${s.collision}</span></div><div><b>Spawn</b><span>${s.spawnPhase} • active ${s.activeCount} • cleared ${s.cleared} • hits ${s.hits}</span></div><div><b>Event</b><span>${s.lastEvent}</span></div>`;}setInterval(render,250);window.CC_APP.onModeChange(render);window.CC_RUNTIME_API?.frame()?.addEventListener("load",render);render();}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",mount,{once:true});else mount();
})();